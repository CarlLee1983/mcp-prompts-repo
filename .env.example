# MCP Prompt Manager 環境變數配置範例
# 複製此檔案為 .env 並填入實際值

# ============================================
# 必填配置
# ============================================

# Git Repository 來源（必填）
# 可以是本地路徑或 Git URL（HTTP/HTTPS/SSH）
# 本地路徑範例:
PROMPT_REPO_URL=/path/to/your/prompts-repo
# Git URL 範例:
# PROMPT_REPO_URL=https://github.com/username/prompts-repo.git
# PROMPT_REPO_URL=git@github.com:username/prompts-repo.git

# ============================================
# 可選配置
# ============================================

# 輸出語言設定（預設: en）
# 可選值: en, zh
MCP_LANGUAGE=en

# 群組過濾設定（預設: common）
# 指定要載入的群組，多個群組用逗號分隔
# 根目錄和 common 群組永遠載入
# 範例: MCP_GROUPS="laravel,vue,react"
MCP_GROUPS=laravel,vue

# 本地儲存目錄（預設: .prompts_cache）
# Git 倉庫會被 clone 到此目錄
STORAGE_DIR=.prompts_cache

# Git 分支（預設: main）
# 指定要 clone 的 Git 分支
GIT_BRANCH=main

# Git 重試次數（預設: 3）
# Git 操作失敗時的最大重試次數
GIT_MAX_RETRIES=3

# 日誌級別（預設: info）
# 可選值: fatal, error, warn, info, debug, trace
# 開發時建議使用 debug，生產環境使用 info
LOG_LEVEL=info
